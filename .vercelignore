# Design and reference material that ships with the repo but never belongs in
# the build bundle. Nothing here is imported by the app, so a dropped asset
# cannot slow or break a deploy.
brand/
docs/
eval/
fixtures/
site/
*.md
!README.md
